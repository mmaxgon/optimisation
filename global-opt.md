# Глобальный иерархический оптимизационный алгоритм

Дана задача оптимизации: $$\begin{aligned}&\text{minimize} \ f(x,y) \\ &\text{subject to} \\  &\quad x \in G_1 \\ & \quad (x,y) \in G_2, \end{aligned}\tag{P}$$которую можно рассматривать как задачу **иерархического принятия решения**: переменные решения $x$ – переменные верхнего уровня, "стратегические" (например, транспортные заказы, которые мы решаем принять в работу), а переменные $y$ – "операционные" переменные решения нижнего уровня, сильно зависящие от выбора $x$ (например, маршруты по выбранным заказам).

Функция $f$ – чёрный ящик, заданная, например, ансамблем прогнозных моделей (random forrest, gradient boosting, neural networks со сложной архитектурой), в общем случае 
- не непрерывная (в частности, не выпуклая, не дифференцируемая)
- дорого вычисляемая
- со сложной, плохо аппроксимируемой структурой

При этом, получить оптимальные решения для задач $$\begin{aligned}&\text{minimize} \ f_1(x) \\ &\text{subject to} \\  &\quad x \in G_1 \end{aligned}$$для любой простой (например, линейной) функции $f_1$ и $$\begin{aligned}&\text{minimize} \ f^x_2(y) \\ &\text{subject to} \\ & \quad (x,y) \in G_2 \end{aligned}$$для любого фиксированного $x \in G_1$ и простой функции $f_2^x$ – просто.

Дополнительно предполагаем, что $G_1$ и $G_2(x) \coloneqq \{y \mid (x,y) \in G_2\}$, $x \in G_1$ содержатся в компактных (многомерных) прямоугольниках $K_1$ и $K_2(x)$ соответственно.

## Эвристический алгоритм поиска оптимального решения (P)

Параметры алгоритма:
- $N \approx 100$ (число исследуемых точек в пространстве).
- $\sigma_i > 0$ (по каждой i-ой компоненте переменных решения) – длина её допустимого интервала.
- $\alpha \in \left(0, 1\right]$ – сжимающий коэффициент поиска.

1. Текущие номер итерации $k \coloneqq 0$.
2. Первоначальное исследование допустимого пространства. Выбираем случайно $N$ значений $a_1,\dots,a_N \in K_1$, где компоненты каждого вектора $a_j$ выбираются из **равномерного** распределения в допустимом интервале соответствующей грани прямоугольника $K_1$. Т.е выборка осуществляется равномерно из прямоугольника $K_1$.
3. Проецируем недопустимые $a_j \notin G_1$ на $G_1$ по max-норме, т.е. решаем для каждого такого j оптимизационную задачу: $$\begin{aligned}&\text{minimize} \ \|x-a_j\|_\infty \\ &\text{subject to} \\  &\quad x \in G_1 \end{aligned}$$Мы, таким образом, получим **допустимые** точки $x_1,\dots,x_N \in G_1$, каждая из которых находится максимально близко к случайно выбранным точкам $a_1,\dots,a_N$.
4. Выбираем случайно $N$ значений $b_1,\dots,b_N \in K_2$, где компоненты каждого вектора $b_j$ выбираются из **равномерного** распределения в допустимом интервале соответствующей грани прямоугольника $K_2(x_j)$. Практически это означает, что для каждого j мы
    - Анализируем множество $G_2(x_j)$ и стараемся получить минимальный компакт $K_2(x_j)$, в котором оно содержится. В худшем случае, это будет прямоугольник из компонент $K_2$, соответствующих переменным $y$.
    - Делаем выборку из полученного $K_2(x_j)$.
5. Проецируем недопустимые $b_j \notin G_2(x_j)$ на $G_2(x_j)$ по max-норме, т.е. решаем для каждого такого j оптимизационную задачу: $$\begin{aligned}&\text{minimize} \ \|y - b_j\|_\infty \\ &\text{subject to} \\  &\quad y \in G_2(x_j) \end{aligned}$$Мы, таким образом, получим **допустимые** точки $x_1,\dots,x_N \in G_1$; $(x_1, y_1),\dots, (x_N, y_N) \in G_2$, каждая из которых находится максимально близко к случайно выбранным точкам $a_1,\dots,a_N$ и $(x_1, b_1),\dots,(x_N, b_N)$ соответственно.
    Если $G_2(x_j) = \emptyset$, то ещё раз выбирается $a_j$ и рассчитывается новый $x_j$, и шаг 5 повторяется для данного $j$.
6. Для каждой пары $(x_j,y_j)$, $j=1,\dots,N$ вычисляем значение функции цели (чёрного ящика) $f(x_j,y_j)$. Выбираем $\frac{N}{2}$ лучших точек, т.е. точек с минимальными значениями $f$. Перенумеруем их: $(x_1, y_1),\dots,(x_{N/2},y_{N/2})$.
7. Увеличим текущий номер итерации $k \coloneqq k + 1$.
8. Получим следующие $\frac{N}{2}$ точек $a_1,\dots,a_{N/2}$ из **нормального** распределения $N(x_1, \alpha^k\sigma),\dots,N(x_{N/2}, \alpha^k\sigma)$ в (уменьшающейся от итерации к итерации) окрестности "хороших" точек $x_1,\dots,x_{N/2}$.
9. Проецируем недопустимые $a_j \notin G_1$, $j=1,\dots,\frac{N}{2}$ на $G_1$ по max-норме, т.е. решаем для каждого такого j оптимизационную задачу: $$\begin{aligned}&\text{minimize} \ \|x-a_j\|_\infty \\ &\text{subject to} \\  &\quad x \in G_1 \end{aligned}$$Получим решения $x_{\frac{N}{2}+1},\dots,x_N \in G_1$.
10. Один из двух вариантов:
    - **На более ранних этапах**. Получим следующие $\frac{N}{2}$ точек $b_1,\dots,b_{N/2}$ из **нормального** распределения $N(y_1, \alpha^k\sigma),\dots,N(y_{N/2}, \alpha^k\sigma)$ в (уменьшающейся от итерации к итерации) окрестности "хороших" точек $y_1,\dots,y_{N/2}$. Проецируем недопустимые $b_j \notin G_2\left(x_{\frac{N}{2} + j}\right)$, $j=1,\dots,\frac{N}{2}$ на $G_2\left(x_{\frac{N}{2} + j}\right)$ по max-норме, т.е. решаем для каждого такого j оптимизационную задачу: $$\begin{aligned}&\text{minimize} \ \|y - b_j\|_\infty \\ &\text{subject to} \\  &\quad y \in G_2\bigl(x_{\frac{N}{2} + j}\bigr) \end{aligned}$$Получим **допустимые** точки $(x_{\frac{N}{2}+1}, y_{\frac{N}{2}+1}),\dots, (x_N, y_N) \in G_2$, которые объединяем с самыми на данный момент лучшими $(x_1,y_1),\dots,(x_{\frac{N}{2}}, y_{\frac{N}{2}})$.
    - **На более поздних этапах**. Для $j=\frac{N}{2}+1,\dots,N$ аппроксимируем $y \mapsto f(x_j, \cdot)$ функцией $\hat{f}_j$ **локально** в окрестности $x_j.$ Т.е. для всех накопленных в истории пар $(x_k, y_k)$ (в том числе и отброшенных ранее "плохих") с условием $\|x_k - x_j\| \leq \delta$ строим взвешенную по расстоянию аппроксимацию:
        + Вычисляем вес точки $x_k$: $w_k = \exp\left(-\frac{\|x_k - x_j\|^2}{2\alpha^k\sigma^2}\right)$
        + Собираем тройки $(x_k, y_k, f_k)$ с весами $w_k$
        + Обучаем **взвешенную Ridge-регрессию**:
        ```Python
        distances_x = np.array([np.linalg.norm(x_k - x_j) for x_k, y_k, f_k in history])
        weights = np.exp(-distances_x**2 / (2 * sigma_x**2))
        
        XY_data = np.array([np.concat((x_k, y_k)) for x_k, y_k, f_k in history])
        F_data = np.array([f_k for x_k, y_k, f_k in history])
        
        # Полиномиальные признаки по x, y
        poly = PolynomialFeatures(degree=2, include_bias=True)
        Phi = poly.fit_transform(XY_data)
        
        # Взвешенная регрессия
        model = Ridge(alpha=1e-6).fit(Phi, F_data, sample_weight=weights)
        ```
        + Получаем коэффициенты квадратичной регрессии при фиксированном $x = x_j$: $\hat{f}_j(y) = a_0 + \sum_i a_i y^{(i)} + \sum_{i,k}a_{i,k} y^{(i)} y^{(k)}$ из модели.
        + Решаем оптимизационную задачу: $$\begin{aligned}&\text{minimize} \ \hat{f}_j(y) \\ &\text{subject to} \\  &\quad y \in G_2(x_j) \end{aligned}$$
      Получим **допустимые** точки $(x_{\frac{N}{2}+1}, y_{\frac{N}{2}+1}),\dots, (x_N, y_N) \in G_2$, которые объединяем с самыми на данный момент лучшими $(x_1,y_1),\dots,(x_{\frac{N}{2}}, y_{\frac{N}{2}})$.
11. goto 6

Плюсами такого подхода является:
- Нахождение на каждой итерации $N/2$ новых допустимых точек.
- Последовательное улучшение найденных решений по исходной функции цели.
- Исследование пространства допустимых решений, постепенно переходящее к исследованию вблизи точек с лучшими значениями ФЦ.
- На каждой итерации решаются менее сложные, чем исходная оптимизационные задачи (при помощи MILP-солвера).
- По мере накопления данных оптимизируется локальная аппроксимация целевой функции.

---

Спасибо за подробное описание вашего алгоритма! Ниже — мой анализ, комментарии и возможные улучшения с точки зрения эффективности, корректности и стабильности. Я также кратко переформулирую суть подхода, чтобы убедиться, что всё понял верно.

***

## 🔍 **Краткое резюме алгоритма**

Вы решаете сложную задачу иерархической глобальной оптимизации с чёрным ящиком $ f(x,y) $, где:

* $ x \in G\_1 $ — стратегические переменные (высокий уровень),
* $ y \in G\_2(x) $ — операционные переменные (низкий уровень),
* $ f $ — недифференцируемая, не гладкая, дорого вычисляемая функция,
* но подзадачи по $ x $ и $ y $ при фиксированных переменных легко решаются (например, MILP/CP).

Алгоритм — **эволюционный, с элементами метамоделирования**:

1. Начинает с равномерной случайной выборки в пространстве $ x $.

2. Для каждого $ x\_j $ генерирует $ y\_j $, проецирует в допустимую область, вычисляет $ f(x\_j, y\_j) $.

3. Сохраняет лучшие $ N/2 $ точек.

4. На каждой итерации:

   * Генерирует новые $ x $ из нормального распределения вокруг лучших решений (с уменьшающейся дисперсией).
   * Проектирует их в $ G\_1 $.
   * Либо генерирует $ y $ случайно около предыдущих, либо строит локальную квадратичную аппроксимацию $ f $ и оптимизирует её по $ y $.

5. Повторяет до сходимости.

Это — **гибридный подход**: случайный поиск + локальная оптимизация на основе модели + адаптивное сужение области поиска.

***

## ✅ **Сильные стороны алгоритма**

1. **Устойчивость к "чёрному ящику"**\
   Не требует градиентов, выпуклости, непрерывности — идеально для ML-моделей в качестве $ f $.

2. **Использование структуры иерархии**\
   Разделение $ x $ и $ y $ позволяет эффективно использовать внутренние оптимизаторы для проекций и локальных задач.

3. **Адаптивный поиск**\
   Сужение дисперсии через $ \alpha^k \sigma $ обеспечивает переход от **исследования** к **эксплуатации**.

4. **Метамодель на поздних стадиях**\
   Использование взвешенной Ridge-регрессии с полиномиальными признаками — разумный компромисс между гибкостью и устойчивостью.

5. **Проекция в допустимую область**\
   Обеспечивает, что все пробные точки остаются допустимыми — критично для сложных ограничений.

6. **Гибкость в выборе стратегии по $ y $**\
   Ранние итерации — случайный поиск, поздние — использование модели — это разумная эвристика.

***

## ⚠️ **Потенциальные проблемы и предложения по улучшению**

### 1. **Проекция по max-норме может быть нефизичной**

> $$ \min |x - a\_j|\_\infty \quad \text{s.t. } x \in G\_1 $$

* **Проблема**: $ |\cdot|\_\infty $ может приводить к "угловым" проекциям, особенно в высоких размерностях. Это может создавать артефакты: например, одна компонента сильно меняется, другие — нет.
* **Решение**: рассмотрите проекцию по $ \ell\_2 $-норме (Евклидова), если $ G\_1 $ позволяет. Она более "гладкая" и физически интерпретируема.
* Альтернатива: использовать **масштабированную норму**, учитывающую разброс $ \sigma\_i $, например: $$ \min \sum\_i \left(\frac{x^{(i)} - a\_j^{(i)}}{\sigma\_i}\right)^2 $$

***

### 2. **Выбор $ \delta $ в локальной аппроксимации не определён**

> `if ||x_k - x_j|| <= delta` — но $ \delta $ не задано.

* **Проблема**: слишком маленькое $ \delta $ — мало данных для обучения; слишком большое — модель становится глобальной, теряет локальность.
* **Решение**: сделать $ \delta = C \cdot \alpha^k \sigma $, т.е. масштабировать с уменьшением окрестности поиска. Например, $ C = 2 $ или $ 3 $.

***

### 3. **Ridge-регрессия с полиномами степени 2 — может недо/переобучать**

* **Недообучение**: если истинная зависимость $ f(x,y) $ — не квадратичная (например, кусочно-линейная от маршрутов), то модель будет неточной.
* **Переобучение**: при малом количестве точек и высокой размерности $ y $, даже степень 2 даёт много параметров.

#### Возможные улучшения:

* Использовать **Gaussian Process (GP)** с RBF-ядром для $ \hat{f}\_j(y) $ — он лучше работает с малыми данными и даёт оценку неопределённости.
* Или **Random Forest** с локальным обучением — более устойчив к выбросам и нелинейностям.
* Оставить Ridge, но добавить **отбор признаков** (например, Lasso в ансамбле) или использовать **PCA перед регрессией**.

> Но учтите: GP медленнее, чем Ridge, особенно при большом $ N $. Если скорость критична — Ridge с регуляризацией — хороший выбор.

***

### 4. **Повторная выборка при $ G\_2(x\_j) = \emptyset $ — может замедлять алгоритм**

> Повторять выборку $ M $ раз — риск зацикливания, если $ G\_1 $ плохо сэмплируется.

#### Улучшение:

* Перед генерацией $ x\_j $ можно **оценить шансы $ G\_2(x\_j) \neq \emptyset $** с помощью быстрой эвристики (например, проверка "достаточно ли заказов для построения маршрута").
* Или использовать **штраф в целевой функции** за недопустимость $ y $, а не перегенерацию.

***

### 5. **Фиксированное деление $ N/2 $ — может быть неоптимальным**

* Почему именно $ N/2 $? Что, если в начале нужно больше исследований, а в конце — меньше?

* **Предложение**: использовать **адаптивный размер популяции**, например:

  * На итерации $ k $: сохранять $ \lceil N / (1 + \beta k) \rceil $ лучших точек.
  * Или использовать **tournament selection** вместо жёсткого отбора.

***

### 6. **Отсутствие критерия останова**

* Алгоритм использует `goto 6`, но не указано, когда останавливаться.

* **Предложения**:

  * Останов при стагнации: если лучшее значение $ f $ не улучшается $ T $ итераций.
  * Останов при $ \alpha^k \sigma < \varepsilon $ — когда шаги становятся слишком малы.
  * Останов по бюджету вызовов $ f $ (дорогая функция!).

***

### 7. **Не используется информация о градиенте (даже приближённом)**

* Хотя $ f $ — чёрный ящик, можно попробовать **оценить направление спуска** через:

  * **Finite differences** в окрестности лучших точек.
  * **CMA-ES**-подобные обновления ковариационной матрицы вместо фиксированного $ \sigma $.

* Это могло бы ускорить сходимость на поздних стадиях.

***

## 🛠️ **Рекомендации по реализации**

1. **Логируйте историю всех $(x\_k, y\_k, f\_k)$** — она нужна для метамодели.
2. **Кэшируйте значения $ f $** — чтобы не пересчитывать при повторных точках.
3. **Параллельно вычисляйте $ f(x\_j, y\_j) $** — так как это самая дорогая часть.
4. **Для проекции в $ G\_1 $ и $ G\_2(x) $ используйте MILP-солвер с warm start** — ускорит работу.
5. **Добавьте визуализацию** (если размерность позволяет): например, t-SNE по $ x $, чтобы видеть, как сходится поиск.

***

## 🔄 Возможная модификация: замена "случайного + модель" на **Bayesian Optimisation (BO)**

Хотя ваш алгоритм уже хорош, можно рассмотреть **BO для $ x $**, а для $ y $ — как сейчас:

* BO оптимизирует $ x $, используя GP как метамодель.
* Для каждого $ x $, решает нижнюю задачу: $ y^\*(x) = \arg\min\_{y \in G\_2(x)} \hat{f}(x,y) $ (через вашу аппроксимацию или симуляцию).
* Вычисляет $ f(x, y^\*(x)) $ — и обновляет GP.

**Плюсы BO**:

* Более систематичный баланс исследования и эксплуатации.
* Меньше параметров (не нужно $ N $, $ \alpha $, $ \sigma $ вручную).

**Минусы**:

* Масштабируется плохо при $ \dim x > 20 $.
* Требует аккуратной настройки ядра.

👉 Ваш подход **гибридный и более масштабируемый** — поэтому, возможно, и лучше для вашей задачи.

***

## ✅ Вывод

Ваш алгоритм — **очень разумный, практичный и ориентированный на реальные ограничения** (дорогая функция, сложные ограничения, иерархия). Он сочетает:

* глобальный поиск,
* адаптивное сужение,
* локальное метамоделирование,
* проекцию в допустимую область.

Он хорошо подходит для логистики, планирования, управления цепями поставок и других приложений с ML-моделями в петле.

***

Если хотите, могу:

* помочь реализовать его на Python (с примером),
* предложить конкретный код для локальной аппроксимации,
* или сравнить с аналогами (CMA-ES, Bayesian Optimization, etc.).

Готов продолжить — просто скажите, в каком направлении двигаться.

